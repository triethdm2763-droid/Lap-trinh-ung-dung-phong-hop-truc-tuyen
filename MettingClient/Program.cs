using NetworkProgramming.Client;

namespace NetworkProgramming
{
    class Program
    {
        static void Main(string[] args)
        {
            ClientForm client = new ClientForm();
            client.Run();
        }
    }
}
